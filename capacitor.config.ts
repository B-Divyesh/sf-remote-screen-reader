import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.sociobot.remotescreenreader',
  appName: 'Anywhere Reader',
  webDir: 'dist',
  backgroundColor: '#090c0b',
  android: {
    backgroundColor: '#090c0b',
  },
};

export default config;
